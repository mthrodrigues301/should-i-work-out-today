{
  "name": "should-i-work-out-today",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "start": "npx serve ."
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
