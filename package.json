{
  "name": "should-i-work-out-today",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "start": "npx serve ."
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^2.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "sharp": "^0.35.4"
  }
}
